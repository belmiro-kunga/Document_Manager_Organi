#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged on Windows (skip if WSL issues)
if command -v npx >/dev/null 2>&1; then
  npx lint-staged
else
  echo "Skipping lint-staged due to environment issues"
fi